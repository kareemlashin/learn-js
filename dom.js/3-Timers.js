function simpleMessage() {
     console.log('This is just a simple alert');
   }
   
   // set time out
   setTimeout(simpleMessage, 1000);
   
  
   // set interval. will repeat every  5000ms
   setInterval(simpleMessage, 1000);
   
   